<style>
  <% data.themes.each do |theme| %>
    .theme-<%= theme.name %> .main-wrapper {
      background-color: <%= theme.color %>;
    }

    .theme-circle-<%= theme.name %> {
      background-color: <%= theme.color %>;
    }
  <% end %>
</style>
